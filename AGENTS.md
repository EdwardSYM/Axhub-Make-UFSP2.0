# Agents 工作流程说明

本项目为目标用户开发原型，需要 Agents 扮演产品经理、 UI/UX设计师和前端开发的复合角色。

## 🧭 工作流程

### 场景 1：新建或更新原型

1. **阅读规范与资料** → 先读 `rules/README.md`，再按规则索引读取用户提供资料、页面治理规范和同类已确认页面
2. **需求对齐（可选）** → 用户要求时启动，澄清需求，确认后才继续
3. **原型/组件设计** → 进入设计流程，确定布局与视觉方向并产出 `spec.md`
4. **开发与验收** → 根据 spec 实现原型/组件，并完成与任务风险匹配的验收

### 场景 2：文档或更新文档

1. **阅读规范与资料** → 先读 `rules/README.md`，再按任务类型读取对应文档规则、用户资料与参考
2. **需求对齐（可选）** → 用户要求时启动，澄清输出目标与范围
3. **生成文档** → 在 `src/docs/` 生成或更新相关文档

### 场景 3：资源管理

1. **阅读资源规则** → 查看资产目录与已有资源
2. **新增/更新资源** → 按规范放置、命名与维护

## 📌 项目信息

- 项目名称：财会监督V2.0

## ⚠️ 重要原则

1. **文档与代码同步**
   - 修改代码时，必须同步更新对应的 `spec.md` 规格文档
   - 修改 `spec.md` 时，必须同步更新对应的代码实现

2. **完整阅读文档资料**
   - 必须仔细阅读用户提供的所有文档和资料
   - 必须查看上下文中提供的相关规则和参考文件
   - 遇到新问题、新场景、样式确认或规范冲突时，必须先按 `rules/README.md` 判断应更新哪个规则、文档、组件或记忆，再向用户说明并确认

3. **页面样式与组件规范渐进沉淀**
   - 新页面默认复用现有强基准、同类已确认页面和项目中复用最多的稳定组件样式
   - 不为了统一样式规范而阻塞当前业务页面推进
   - 用户在具体页面中确认的样式，先判断属于当前页局部、页面范式还是组件规范，再决定是否沉淀到 `confirmed-baselines.md`、`ufsp-page-governance.md`、当前页面 `spec.md` 或公共组件
   - 已确认组件样式属于冻结基线，后续业务需求变化不得顺手改动
   - 现有组件无法满足核心业务时，必须先说明原因、影响范围和替代方案，等待用户确认后再新增组件或组件变体
   - 不默认批量同步旧页面；只有确认是组件级规范且不会覆盖其他已确认冻结区时，才考虑同步

4. **主动沉淀记忆**
   - 在生成或修改原型、文档等内容后，Agent 必须主动给用户一条沉淀记忆建议
   - 对用户的表达应尽量自然，优先使用“我可以继续帮你整理并记住 xxx”这类大众容易理解的话术
   - 建议应明确说明：还可以继续补充维护项目文档、主题或数据，并尽量给出 1-3 个具体例子
   - 只有用户明确同意后，Agent 才读取 `rules/memory-system-guide.md` 并开始执行主动沉淀记忆工作

5. **自主完成所有操作**
   - 用户不懂开发技术，无法执行任何 CLI 命令
   - 不得省略与任务风险匹配的验收；静态原型小改可做轻量文件自查，复杂原型或运行错误按规则执行验收脚本或调试流程

## 📄 详细文档

| 阶段 | 文档 | 用途 |
|------|------|------|
| 规则索引 | `rules/README.md` | 统一查看全部规则命名、用途与检索入口 |
| 页面治理 | `rules/ufsp-page-governance.md` | 新页面、页面优化、组件规范、样式同步与规范迭代的主规则 |
| 基线登记 | `rules/confirmed-baselines.md` | 已确认页面、组件样式、局部基准、待确认参考和反例登记 |
| 原型/组件设计 | `rules/design-guide.md` | 设计流程与规格文档产出 |
| 文档生成 | `rules/documentation-guide.md` | 项目文档时生成和编辑 |
| 代码开发 | `rules/development-guide.md` | 代码规范和验收流程 |
| 主动沉淀记忆 | `rules/memory-system-guide.md` | 文档、主题、数据等后置资源的持续记忆沉淀流程 |
| 资源管理 | `rules/resource-management-guide.md` | 资产新增、引用与维护 |

## 项目结构
```
├── src/
│   ├── common/        # 公共类型和工具
│   ├── components/    # 组件目录
│   ├── prototypes/    # 原型目录
│   ├── docs/          # 项目文档
│   ├── themes/        # 主题配置
│   └── database/      # 页面可直接消费的数据表
└── skills/            # 项目技能与工作流
```
