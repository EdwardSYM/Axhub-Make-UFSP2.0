# 典型案例管理-申请原型规格

本页面是 `案例库（AI改造）` 的二级入口，复用 `src/prototypes/case-library-ai` 页面实现并固定进入 `feature=typical_case_application`。

申请页用于填报侧维护典型案例申请，状态收口为“待提交、被退回、已发布、全部”，支持新增、从聚类分析生成、批量提交、编辑详情和查看发布记录。详细字段、视觉规范和交互口径以 `src/prototypes/case-library-ai/spec.md` 中“典型案例申请页”说明为准。
