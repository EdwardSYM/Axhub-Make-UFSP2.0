# 典型案例管理-审核原型规格

本页面是 `案例库（AI改造）` 的二级入口，复用 `src/prototypes/case-library-ai` 页面实现并固定进入 `feature=typical_case_review`。

审核页用于审核侧处理已提交的典型案例，状态收口为“待审核、被退回、已发布、全部”，支持批量审核通过、退回、审核详情和查看发布记录。详细字段、视觉规范和交互口径以 `src/prototypes/case-library-ai/spec.md` 中“典型案例审核页”说明为准。
